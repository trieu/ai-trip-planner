# This file lists the dependencies for the backend of the AI Trip Planner application.

#  FASTAPI for building the web API
#  HTTPX for making async HTTP requests to external services
fastapi
fastapi-limiter
uvicorn[standard]
python-dotenv
httpx
tenacity

# Data validation and settings management
pydantic>=2.7.4
pydantic-settings
# Email utilities for user notifications and alerts
pydantic[email]

# Authentication & Security
PyJWT
passlib
argon2-cffi
python-multipart

# AI and LLM related
langchain>=1.2.17
langchain-community>=0.4.1
langgraph>=1.1.8
langchain-google-genai>=4.2.2
langchain-openai>=1.2.1
tavily-python>=0.7.23
google-genai>=2.0.1

# Observability
arize-phoenix
opentelemetry-api
opentelemetry-sdk
opentelemetry-exporter-otlp
openinference-instrumentation-langchain

# Database
sqlalchemy>=2.0
greenlet # Required for SQLAlchemy async support
asyncpg # Async driver for PostgreSQL, required by SQLAlchemy for async operations
psycopg[binary] # Synchronous driver for PostgreSQL
pgvector # For vector search capabilities in PostgreSQL
python-arango # ArangoDB driver to connect LEO CDP directly
alembic>=1.18.0 # Database Migrations
redis[hiredis]>= 6.0.0 # Redis client for asynchronous operations

 # Distributed task queue with Redis as broker, for async jobs: embeddings, batch processing
dramatiq[redis, watch]
periodiq # 
scrapling[fetchers]  # For web scraping tasks, if needed for data collection or enrichment

# Development & Testing
pytest>=7.4.3
pytest-asyncio>=0.21.1
black>=23.12.0
flake8>=6.1.0
mypy>=1.7.1